"use client";
import { useState } from "react"; import { AlertCircle, ArrowLeft, ArrowRight, Loader2, X } from "lucide-react"; import FleetRegistrationStepper, { STEPS } from "./FleetRegistrationStepper"; import { EMPTY_FLEET_DRAFT, type FleetRegistrationDraft, type FleetStep } from "./types"; import { registerFleet } from "./api"; import { validateFleetDraft, validateFleetStep } from "./validation"; import VehicleDetailsStep from "./steps/VehicleDetailsStep"; import SeatLayoutStep from "./steps/SeatLayoutStep"; import VehiclePhotosStep from "./steps/VehiclePhotosStep"; import DocumentsStep from "./steps/DocumentsStep"; import RouteAssignmentStep from "./steps/RouteAssignmentStep"; import ReviewStep from "./steps/ReviewStep";
export default function FleetRegistrationFlow({ open, onClose, onRegistered }: { open: boolean; onClose: () => void; onRegistered: (fleetId: string) => void }) { const [step, setStep] = useState<FleetStep>("vehicle"); const [completed, setCompleted] = useState<FleetStep[]>([]); const [draft, setDraft] = useState<FleetRegistrationDraft>(EMPTY_FLEET_DRAFT); const [serverFleetId, setServerFleetId] = useState<string | undefined>(); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); if (!open) return null; const index = STEPS.findIndex((item) => item.id === step);
  function next() { const issue = validateFleetStep(step, draft); if (issue) return setError(issue); setError(null); setCompleted((items) => items.includes(step) ? items : [...items, step]); if (index < STEPS.length - 1) setStep(STEPS[index + 1].id); }
  async function submit() { const issue = validateFleetDraft(draft); if (issue) return setError(issue); setBusy(true); setError(null); try { const id = await registerFleet(draft, serverFleetId, setServerFleetId); setDraft(EMPTY_FLEET_DRAFT); setServerFleetId(undefined); setCompleted([]); setStep("vehicle"); onRegistered(id); onClose(); } catch (e) { setError(`${e instanceof Error ? e.message : "Unable to submit fleet registration."}${serverFleetId ? " Your server draft is preserved; retry will continue the same vehicle." : ""}`); } finally { setBusy(false); } }
  const content = step === "vehicle" ? <VehicleDetailsStep draft={draft} update={setDraft} /> : step === "layout" ? <SeatLayoutStep draft={draft} update={setDraft} /> : step === "photos" ? <VehiclePhotosStep draft={draft} update={setDraft} /> : step === "documents" ? <DocumentsStep draft={draft} update={setDraft} /> : step === "route" ? <RouteAssignmentStep draft={draft} update={setDraft} /> : <ReviewStep draft={draft} />;
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/55 p-3 backdrop-blur-sm"><div role="dialog" aria-modal="true" aria-label="Register fleet" className="flex max-h-[96vh] w-full max-w-6xl flex-col overflow-hidden rounded-[28px] border border-[#E8E1DB] bg-white shadow-2xl"><header className="flex items-center justify-between gap-4 border-b border-[#E8E1DB] px-5 py-4 sm:px-7"><div><p className="text-[10px] font-black uppercase tracking-[0.18em] text-[#7A1D1B]">Fleet registration</p><h2 className="mt-1 text-xl font-black text-[#191512]">Prepare one review-ready vehicle</h2></div><button type="button" onClick={onClose} disabled={busy} className="flex size-10 items-center justify-center rounded-xl border border-[#E8E1DB]" aria-label="Close"><X className="size-4" /></button></header><FleetRegistrationStepper active={step} completed={completed} onSelect={(value) => { setError(null); setStep(value); }} /><main className="min-h-0 flex-1 overflow-y-auto px-5 py-6 sm:px-7">{error && <div className="mb-5 flex gap-2 rounded-2xl border border-red-200 bg-red-50 p-4 text-xs font-bold text-red-700"><AlertCircle className="size-4 shrink-0" />{error}</div>}{content}</main><footer className="flex items-center justify-between border-t border-[#E8E1DB] bg-white px-5 py-4 sm:px-7"><button type="button" onClick={() => index > 0 && setStep(STEPS[index - 1].id)} disabled={index === 0 || busy} className="flex h-11 items-center rounded-xl border border-[#DCD4CD] px-4 text-xs font-black disabled:opacity-30"><ArrowLeft className="mr-2 size-4" />Back</button>{step === "review" ? <button type="button" onClick={() => void submit()} disabled={busy} className="flex h-11 items-center rounded-xl bg-[#7A1D1B] px-5 text-xs font-black text-white disabled:opacity-50">{busy && <Loader2 className="mr-2 size-4 animate-spin" />}{busy ? "Submitting…" : "Submit for review"}</button> : <button type="button" onClick={next} className="flex h-11 items-center rounded-xl bg-[#191512] px-5 text-xs font-black text-white">Continue<ArrowRight className="ml-2 size-4" /></button>}</footer></div></div>; }
