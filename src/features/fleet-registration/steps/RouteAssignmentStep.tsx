import FormField, { inputClass } from "../components/FormField"; import type { FleetRegistrationDraft } from "../types";
export default function RouteAssignmentStep({ draft, update }: { draft: FleetRegistrationDraft; update: (next: FleetRegistrationDraft) => void }) { const set = (key: keyof FleetRegistrationDraft["route"], value: string) => update({ ...draft, route: { ...draft.route, [key]: value } }); return <div className="space-y-5"><div className="grid gap-5 sm:grid-cols-2"><FormField label="From"><input className={inputClass} value={draft.route.origin} onChange={(e) => set("origin", e.target.value)} placeholder="Kathmandu" /></FormField><FormField label="To"><input className={inputClass} value={draft.route.destination} onChange={(e) => set("destination", e.target.value)} placeholder="Pokhara" /></FormField></div><FormField label="Via" hint="Optional"><input className={inputClass} value={draft.route.viaStops} onChange={(e) => set("viaStops", e.target.value)} placeholder="Mugling, Dumre" /></FormField></div>; }
