import FormField, { inputClass } from "../components/FormField"; import type { FleetRegistrationDraft } from "../types";
export default function RouteAssignmentStep({ draft, update }: { draft: FleetRegistrationDraft; update: (next: FleetRegistrationDraft) => void }) { const set = (key: keyof FleetRegistrationDraft["route"], value: string) => update({ ...draft, route: { ...draft.route, [key]: value } }); return <div className="space-y-5"><div className="rounded-2xl border border-[#ECD3CD] bg-[#FFF3F0] p-4 text-xs leading-5 text-[#655E58]">Enter the DOTM-approved route written on this vehicle’s permit. Shuvmarg will match it to the platform corridor; this does not let the operator invent a live route.</div><div className="grid gap-5 sm:grid-cols-2"><FormField label="Approved origin"><input className={inputClass} value={draft.route.origin} onChange={(e) => set("origin", e.target.value)} placeholder="Kathmandu" /></FormField><FormField label="Approved destination"><input className={inputClass} value={draft.route.destination} onChange={(e) => set("destination", e.target.value)} placeholder="Pokhara" /></FormField></div><FormField label="Permit via / major places" hint="Comma-separated, exactly as recognized by the permit or operator."><input className={inputClass} value={draft.route.viaStops} onChange={(e) => set("viaStops", e.target.value)} placeholder="Mugling, Dumre" /></FormField></div>; }
