import FormField, { inputClass } from "../components/FormField";
import type { FleetRegistrationDraft } from "../types";
export default function VehicleDetailsStep({ draft, update }: { draft: FleetRegistrationDraft; update: (next: FleetRegistrationDraft) => void }) { const set = (key: keyof FleetRegistrationDraft["vehicle"], value: string) => update({ ...draft, vehicle: { ...draft.vehicle, [key]: value } }); return <div className="grid gap-5 sm:grid-cols-2"><FormField label="Vehicle name"><input className={inputClass} value={draft.vehicle.busName} onChange={(e) => set("busName", e.target.value)} placeholder="Himalayan Express" /></FormField><FormField label="Registration / plate number"><input className={inputClass} value={draft.vehicle.busNumber} onChange={(e) => set("busNumber", e.target.value.toUpperCase())} placeholder="BA 3 KHA 1234" /></FormField><FormField label="Service class"><select className={inputClass} value={draft.vehicle.busType} onChange={(e) => set("busType", e.target.value)}>{["DELUXE","AC","NON_AC","SLEEPER","SEMI_SLEEPER"].map((item) => <option key={item}>{item}</option>)}</select></FormField><FormField label="Vehicle category"><select className={inputClass} value={draft.vehicle.vehicleType} onChange={(e) => set("vehicleType", e.target.value)}>{["BUS","MINIBUS","HIACE","JEEP"].map((item) => <option key={item}>{item}</option>)}</select></FormField><FormField label="Registration year" hint="Optional; use the year shown in the bluebook."><input type="number" min="1980" className={inputClass} value={draft.vehicle.registrationYear} onChange={(e) => set("registrationYear", e.target.value)} /></FormField><div className="rounded-2xl border border-[#ECD3CD] bg-[#FFF3F0] p-4 text-xs leading-5 text-[#655E58]">Seat count is calculated from the physical layout in the next step. It is not typed separately.</div></div>; }
