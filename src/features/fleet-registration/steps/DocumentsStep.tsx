import UploadCard from "../components/UploadCard"; import { inputClass } from "../components/FormField"; import type { FleetRegistrationDraft } from "../types";
export default function DocumentsStep({ draft, update }: { draft: FleetRegistrationDraft; update: (next: FleetRegistrationDraft) => void }) { const file = (key: keyof FleetRegistrationDraft["files"], files: File[]) => update({ ...draft, files: { ...draft.files, [key]: files[0] || null } }); const meta = (key: keyof FleetRegistrationDraft["documents"], value: string) => update({ ...draft, documents: { ...draft.documents, [key]: value } }); return <div className="grid gap-4 lg:grid-cols-2"><UploadCard label="Fitness certificate" files={draft.files.fitnessCert ? [draft.files.fitnessCert] : []} onChange={(f) => file("fitnessCert", f)}><input aria-label="Fitness valid until" type="date" className={inputClass} value={draft.documents.fitnessValidTill} onChange={(e) => meta("fitnessValidTill", e.target.value)} /></UploadCard><UploadCard label="Vehicle insurance" files={draft.files.insurance ? [draft.files.insurance] : []} onChange={(f) => file("insurance", f)}><input aria-label="Insurance policy number" className={inputClass} placeholder="Policy number" value={draft.documents.insurancePolicyNumber} onChange={(e) => meta("insurancePolicyNumber", e.target.value)} /><input aria-label="Insurance valid until" type="date" className={inputClass} value={draft.documents.insuranceValidTill} onChange={(e) => meta("insuranceValidTill", e.target.value)} /></UploadCard><UploadCard label="Bluebook" files={draft.files.bluebook ? [draft.files.bluebook] : []} onChange={(f) => file("bluebook", f)} /><UploadCard label="Route permit" files={draft.files.routePermit ? [draft.files.routePermit] : []} onChange={(f) => file("routePermit", f)}><input aria-label="Route permit valid until" type="date" className={inputClass} value={draft.documents.routePermitValidTill} onChange={(e) => meta("routePermitValidTill", e.target.value)} /></UploadCard></div>; }
