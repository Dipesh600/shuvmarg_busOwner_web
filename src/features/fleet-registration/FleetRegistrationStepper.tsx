import { BusFront, Camera, Check, FileText, MapPin, Rows3 } from "lucide-react"; import type { FleetStep } from "./types";
export const STEPS: Array<{ id: FleetStep; label: string; icon: typeof BusFront }> = [{ id: "vehicle", label: "Vehicle", icon: BusFront }, { id: "layout", label: "Seat layout", icon: Rows3 }, { id: "photos", label: "Photos", icon: Camera }, { id: "documents", label: "Documents", icon: FileText }, { id: "route", label: "Route", icon: MapPin }, { id: "review", label: "Review", icon: Check }];
export default function FleetRegistrationStepper({ active, completed, onSelect }: { active: FleetStep; completed: FleetStep[]; onSelect: (step: FleetStep) => void }) { return <nav className="grid grid-cols-3 gap-1 border-b border-[#E8E1DB] bg-[#FAF8F5] p-3 sm:grid-cols-6">{STEPS.map(({ id, label, icon: Icon }, index) => { const selected = id === active; const done = completed.includes(id); return <button type="button" key={id} onClick={() => onSelect(id)} disabled={!done && !selected} className={`flex min-w-0 items-center gap-2 rounded-xl px-2 py-2.5 text-left text-[10px] font-black transition ${selected ? "bg-[#7A1D1B] text-white" : done ? "bg-white text-[#211D1A]" : "text-[#AAA19A]"}`}><span className={`flex size-6 shrink-0 items-center justify-center rounded-lg ${selected ? "bg-white/15" : "bg-[#F2ECE7]"}`}>{done && !selected ? <Check className="size-3.5 text-emerald-700" /> : <Icon className="size-3.5" />}</span><span className="truncate"><span className="hidden sm:inline">{index + 1}. </span>{label}</span></button>; })}</nav>; }
