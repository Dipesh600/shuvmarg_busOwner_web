import { FileCheck2, Upload } from "lucide-react";
export default function UploadCard({ label, files, multiple = false, accept = "application/pdf,image/jpeg,image/png,image/webp", onChange, children }: { label: string; files: File[]; multiple?: boolean; accept?: string; onChange: (files: File[]) => void; children?: React.ReactNode }) { return <div className="rounded-2xl border border-[#E8E1DB] bg-[#FFFCFA] p-4"><div className="flex items-center justify-between"><p className="text-xs font-black text-[#211D1A]">{label}</p>{files.length ? <span className="flex items-center gap-1 text-[10px] font-bold text-emerald-700"><FileCheck2 className="size-3.5" />{files.length} ready</span> : <span className="text-[10px] font-bold text-[#938A82]">Required</span>}</div><label className="mt-3 flex cursor-pointer items-center justify-center rounded-xl border border-dashed border-[#D6CBC4] bg-white px-3 py-4 text-xs font-bold text-[#7A1D1B] hover:bg-[#FFF6F2]"><Upload className="mr-2 size-4" />Choose {multiple ? "files" : "file"}<input className="sr-only" type="file" accept={accept} multiple={multiple} onChange={(event) => onChange(Array.from(event.target.files || []))} /></label>{files.length > 0 && <p className="mt-2 truncate text-[10px] text-[#746E69]">{files.map((file) => file.name).join(", ")}</p>}{children}</div>; }
